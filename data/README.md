# data/

Sample datasets, local knowledge bases, and evaluation fixtures live here.

- `knowledge_base.json` -- sample entries used by the `search_knowledge_base` tool.

Keep large or sensitive datasets out of git; add a `.gitignore` rule here
if you start storing real data.
